import { PanelPlaceholder } from "../components/PanelPlaceholder";

export function NotesPage() {
  return (
    <PanelPlaceholder
      eyebrow="Think in one place"
      title="Notes"
      description="Notes are not available."
    />
  );
}
